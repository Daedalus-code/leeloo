#!/bin/bash

source /usr/local/bin/include/leeloo/broker.conf

# run all strategies installed
for f in "${SYS_DIR:?}"/robot/*.strategy; do
  echo "$DATE :: Checking strategies ::" >>"${LOG_DIR:?}"/leeloo/robot.log
  bash "$f"
  sleep "${BOT_TIMER:?}"
done

# END
