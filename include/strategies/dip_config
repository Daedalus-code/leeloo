#!/bin/bash

# shellcheck disable=SC2034  # Unused variables left for readability

################################################################################

# configuration
source /usr/local/bin/include/leeloo/broker.conf

################################################################################
# dip strategy configuration
################################################################################

# SXC southxchange template
# XGX xeggex template

################################################################################

EXCHANGE_API=""        # exchange api (template)
DIP_MIN_DIP="2"        # minimum dip percentage (slightly higher for added caution)
DIP_MAX_DIP="80"       # maximum dip percentage (adjust based on market conditions)
DIP_MIN_SPREAD="8"     # minimum spread percentage (lower for higher liquidity)
DIP_MAX_SPREAD="60"    # maximum spread percentage (be cautious in volatile markets)
DIP_MIN_VOLUME="2.0"   # minimum volume (higher for liquidity)
DIP_MIN_MOVEMENT="0"   # minimum price movement (filter out minor fluctuations)
DIP_MARKETS=""         # leave empty to consider all available markets
DIP_MARGIN="1"         # order margin (slightly higher for added leverage)
DIP_DCA="50"           # dollar Cost Averaging (DCA) percentage (half allocation for DCA)
DIP_ALL_IN="0"         # do not use the reference as targets (maintain independent strategy)
DIP_ALLOCATED="3"      # dip allocated (allocate 3% of the balance)
DIP_MAX_ORDERS="100"   # maximum open orders (manage exposure)
DIP_MAX_AGE="180"      # dip max age (react faster to changing market conditions)
DIP_DISREGARD_BOTS="1" # disregard other markets run by other bots (focus on own strategy)
DIP_OLD_ORDER_MAX="3"  # max buy order age (days) before reconsideration

################################################################################
# strategy
################################################################################

# get strategy id.config, run strategy
BASENAME_STRATEGY="$(basename "$0" | awk -F. '{ print $1 }')"
bash "${SYS_DIR:?}"/robot/"$BASENAME_STRATEGY".strategy

# END
